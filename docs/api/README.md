# API Documentation & OpenAPI Specification

All endpoints are prefixed with `/api/v1` and document their contracts via Swagger OpenAPI 3.0 at `/api/docs`.

## Interactive Swagger
Once the API is running, visit:
`http://localhost:3001/api/docs`
